{% extends 'base.html.twig' %}

{% block header_title %}
<h1>Bienvenue sur Todo List</h1>
{% endblock %}

{% block body %}

<div class="row ">
    <ul class="log">
        <a href="{{ path('task_create') }}" class="btn btn-primary">Créer une nouvelle tâche</a>
        <a href="{{ path('task_todo') }}" class="btn btn-warning">Consulter la liste des tâches à faire</a>
        <a href="{{ path('task_done') }}" class="btn btn-success">Consulter la liste des tâches terminées</a>
        <a href="{{ path('task_list') }}" class="btn btn-secondary">Consulter la liste des tâches</a>
    </ul>
</div>
{% endblock %}