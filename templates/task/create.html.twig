{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    {{ form_start(form) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success pull-right">Ajouter</button>
    {{ form_end(form) }}
    <ul class="log text-center">
        <a href="{{ path('task_list') }}" class="btn btn-primary">Retour à la liste des tâches</a>
    </ul>
</div>
{% endblock %}