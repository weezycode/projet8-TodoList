{% extends 'base.html.twig' %}

{% block body %}
<div class="row">
    {{ form_start(form}) }}
    {{ form_widget(form) }}

    <button type="submit" class="btn btn-success pull-right">Modifier</button>
    {{ form_end(form) }}
</div>
{% endblock %}